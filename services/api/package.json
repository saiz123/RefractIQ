{
  "name": "@agentforge/api",
  "version": "0.1.0",
  "description": "AgentForge HTTP API — Hono",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "node --watch dist/index.js",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agentforge/shared": "workspace:*",
    "@hono/node-server": "^1.12.0",
    "@libsql/client": "^0.14.0",
    "drizzle-orm": "^0.33.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.0",
    "typescript": "^5.5.0"
  }
}
