{
  "name": "@refractiq/orchestrator",
  "version": "0.1.0",
  "description": "Pipeline state machine — sequences agent stages and controls all artifact handoffs",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@refractiq/context-engine": "workspace:*",
    "@refractiq/cost-engine": "workspace:*",
    "@refractiq/model-router": "workspace:*",
    "@refractiq/providers": "workspace:*",
    "@refractiq/shared": "workspace:*",
    "@refractiq/token-engine": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^6.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
